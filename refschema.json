{
    "$schema": "http://json-schema.org/schema#",
    "properties": {
        "$ref": {
            "type": "string",
            "format": "url"
        }
    },
    "required": ["$ref"],
    "type": "object"
}
